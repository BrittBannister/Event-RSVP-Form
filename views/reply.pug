doctype
html
    head
        title= title
    body
        h1= 'Thank you for your response!'
        form(action="/guests" method = "GET")
            button(type="submit")="View Guest List"
        form(action="/" method="GET")
            button(type="submit")= "HOME"
